abstract project Shared is
   package Compiler is
      for Switches ("Ada") use ("-g", "-gnatygO", "-gnatwaCJe", "-gnateE",
                                "-gnatn", "-O2");
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-E", "-g");
   end Binder;

   package Linker is
   end Linker;

end Shared;
