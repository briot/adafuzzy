with "fuzzy";
with "shared";
with "gnatcoll";

project Default is
   for Main use ("example.adb", "robot.adb");
   for Source_Dirs use ("examples");
   for Object_Dir use "obj";

   package Compiler renames Shared.Compiler;
   package Binder renames Shared.Binder;
   package Linker renames Shared.Linker;
end Default;
